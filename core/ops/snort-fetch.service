[Unit]
Description=Snort fetch modules
After=network.target

[Service]
Type=oneshot
EnvironmentFile=/etc/snort/snort.env
ExecStart=/usr/bin/make -C "$SNORT_ROOT/core" fetch

[Install]
WantedBy=multi-user.target

